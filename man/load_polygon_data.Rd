% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geographic-data.R
\name{load_polygon_data}
\alias{load_polygon_data}
\title{Load and standardize polygon data}
\usage{
load_polygon_data(
  data_path,
  id_column,
  target_crs = NULL,
  filter_column = NULL,
  filter_values = NULL
)
}
\arguments{
\item{data_path}{Character scalar giving the spatial vector file path.}

\item{id_column}{Character scalar naming the source identifier column.}

\item{target_crs}{Optional CRS accepted by \code{\link[sf:st_crs]{sf::st_crs()}}, such as an EPSG
code, WKT string, or \code{crs} object. When \code{NULL}, the source CRS is retained.}

\item{filter_column}{Optional character scalar naming a column used to
select features.}

\item{filter_values}{Optional atomic vector containing the values to retain
from \code{filter_column}. It must be supplied together with \code{filter_column}.}
}
\value{
An \code{sf} object containing the selected polygon features. A character
column named \code{polygon_id}, copied from \code{id_column}, is placed first. Source
attributes and the active geometry column are preserved.
}
\description{
Reads polygon features from a spatial vector file, optionally filters them,
validates their identifiers and geometries, repairs invalid geometries, and
optionally transforms them to a target coordinate reference system (CRS).
}
\details{
The function accepts \code{POLYGON} and \code{MULTIPOLYGON} geometries. Empty
geometries, missing identifiers, duplicated identifiers, absent filter
values, and missing source CRS information cause an error. Geometry repair
is performed with \code{\link[sf:st_make_valid]{sf::st_make_valid()}} before any CRS transformation.
}
\examples{
\dontrun{
municipalities <- load_polygon_data(
  data_path = "path/to/municipalities.shp",
  id_column = "CD_MUN",
  target_crs = 4326,
  filter_column = "SIGLA_UF",
  filter_values = "RS"
)
}

}
