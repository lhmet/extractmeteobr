% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate-monthly.R
\name{aggregate_daily_netcdf_by_month}
\alias{aggregate_daily_netcdf_by_month}
\title{Aggregate a daily climate NetCDF file into monthly values}
\usage{
aggregate_daily_netcdf_by_month(
  data_file,
  aggregation_function = "sum",
  overwrite = TRUE
)
}
\arguments{
\item{data_file}{Character scalar giving a daily NetCDF file readable by
\code{\link[terra:rast]{terra::rast()}}. Its variable name is inferred from the file name with
an internal file-name parser.}

\item{aggregation_function}{Character scalar naming a summary function
supported by \code{\link[terra:tapp]{terra::tapp()}}, such as \code{"sum"} or \code{"mean"}.}

\item{overwrite}{Logical scalar. If \code{FALSE} and the output already exists,
its path is returned without recomputation.}
}
\value{
Character scalar giving the monthly NetCDF path.
}
\description{
Reads one daily BR-DWGD NetCDF, aggregates its layers by calendar month,
and writes the result to a NetCDF file inside a \verb{monthly/} subdirectory.
}
\details{
The raster time dimension must contain one non-missing and unique date per
layer, with consecutive one-day intervals. Dates are floored to the first
day of their calendar month and used as the grouping index in
\code{\link[terra:tapp]{terra::tapp()}}.

\code{\link[terra:tapp]{terra::tapp()}} applies the function named by \code{aggregation_function} with
\code{na.rm = TRUE}. BR-DWGD fields are assumed to be spatially and temporally
continuous over the processing domain; therefore, no coverage threshold is
applied. When \code{aggregation_function = "sum"}, the spatial \code{NA} pattern from
the first daily layer is reapplied with \code{\link[terra:mask]{terra::mask()}}. This prevents cells
outside the valid BR-DWGD domain from becoming observed zeros because
\code{sum(..., na.rm = TRUE)} returns zero when all inputs are \code{NA}.

After a successful write, the elapsed wall-clock time is reported for the
input file. The measurement includes reading, aggregation, and NetCDF
writing. The native \code{terra} progress bar is temporarily disabled to avoid
graphical-device redraw warnings in IDEs. The previous \code{terra} progress
setting is restored when the function exits.
}
\examples{
\dontrun{
monthly_pr <- aggregate_daily_netcdf_by_month(
  data_file = "path/to/pr_20200101_20201231_BR-DWGD.nc",
  aggregation_function = "sum",
  overwrite = TRUE
)
}

}
\seealso{
\code{\link[=aggregate_daily_netcdfs_by_month]{aggregate_daily_netcdfs_by_month()}}
}
