% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fill-missing-raster-cells.R
\name{fill_missing_raster_cells}
\alias{fill_missing_raster_cells}
\title{Fill missing raster cells intersecting polygons}
\usage{
fill_missing_raster_cells(rasters, polygons, window_size = 5L, power = 2)
}
\arguments{
\item{rasters}{Named list of SpatRaster objects.}

\item{polygons}{An sf object or SpatVector containing the polygons.}

\item{window_size}{Positive odd integer defining the focal window size.}

\item{power}{Positive numeric scalar defining the inverse-distance power.}
}
\value{
A named list of SpatRaster objects in the same order as rasters.
Rasters requiring no interpolation are returned unchanged.
}
\description{
Checks the first layer of the first raster for NA cells intersecting the
supplied polygons. If at least one such cell exists, all rasters are
interpolated; otherwise, the original list is returned unchanged.
}
\details{
The test uses only the first layer of the first raster and therefore assumes
that all rasters share the same spatial NA mask. When interpolation is
needed, terra::focal() processes all layers but modifies only missing cells.
}
\examples{
\dontrun{
filled_rasters <- fill_missing_raster_cells(
  rasters = monthly_rasters,
  polygons = municipalities
)
}

}
