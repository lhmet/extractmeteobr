% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate-monthly.R
\name{aggregate_daily_netcdfs_by_month}
\alias{aggregate_daily_netcdfs_by_month}
\title{Aggregate BR-DWGD daily NetCDF files into monthly NetCDF files}
\usage{
aggregate_daily_netcdfs_by_month(
  data_path,
  variables = c("pr", "ETo"),
  overwrite = FALSE,
  aggregation_functions = list(pr = "sum", ETo = "sum")
)
}
\arguments{
\item{data_path}{Character scalar giving the directory that contains the
daily BR-DWGD NetCDF files. There is no default; the caller must supply
the directory explicitly.}

\item{variables}{Character vector containing the variable prefixes to
process, such as \code{c("pr", "ETo")}.}

\item{overwrite}{Logical scalar. If \code{FALSE}, an existing output file is
reused. If \code{TRUE}, it is recomputed.}

\item{aggregation_functions}{Named list containing one aggregation-function
name for each entry in \code{variables}. The defaults use \code{"sum"} to calculate
monthly totals for precipitation (\code{pr}) and reference evapotranspiration
(\code{ETo}).}
}
\value{
Character vector containing the monthly NetCDF paths, one for each
daily input file.
}
\description{
Finds the daily NetCDF files for each requested variable, aggregates their
layers by calendar month, and writes one monthly NetCDF beside each input
file, inside a \verb{monthly/} subdirectory.
}
\details{
File matching is literal: a file belongs to a variable only when its base
name starts with \verb{<variable>_}. Files already prefixed with
\verb{<variable>_mly_} are excluded.

Monthly sums retain the spatial \code{NA} pattern of the daily BR-DWGD grid, so
cells outside the valid domain are not converted to zeros.

\code{overwrite = FALSE} only checks whether the output exists; it does not check
which aggregation function created that file. Set \code{overwrite = TRUE} after
changing \code{aggregation_functions}.

The elapsed processing time is reported separately for each input file. It
covers raster reading, monthly aggregation, and NetCDF writing. Files reused
with \code{overwrite = FALSE} are not timed because no processing is performed.
}
\examples{
\dontrun{
monthly_files <- aggregate_daily_netcdfs_by_month(
  variables = c("pr", "ETo"),
  data_path = "path/to/br-dwgd/daily",
  aggregation_functions = list(pr = "sum", ETo = "sum")
)
}

}
\references{
\href{https://sites.google.com/site/alexandrecandidoxavierufes/brazilian-daily-weather-gridded-data}{BR-DWGD author page}, including version information and downloads:
\href{https://drive.google.com/drive/folders/11-qnvwojirAtaQxSE03N0_SUrbcsz44N}{ZIP archives} and \href{https://www.dropbox.com/scl/fo/t225fii1ir4o5ozga0o3u/ALZsq0F4zeCykN4GgoD8D6s?dl=0&rlkey=93nkonzxn08c4wioztkjq0x52&st=oq6g1i0m}{NetCDF files}.

Xavier, A. C., Scanlon, B. R., King, C. W., and Alves, A. I. (2022).
New improved Brazilian daily weather gridded data (1961–2020).
International Journal of Climatology, 42(16), 8390–8404.
DOI: \href{https://doi.org/10.1002/joc.7731}{10.1002/joc.7731}.
}
