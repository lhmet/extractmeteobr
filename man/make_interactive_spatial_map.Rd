% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interactive-maps.R
\name{make_interactive_spatial_map}
\alias{make_interactive_spatial_map}
\title{Create an interactive map of polygons and an optional raster}
\usage{
make_interactive_spatial_map(
  polygons,
  popup_vars,
  popup_title,
  raster = NULL,
  raster_label = NULL,
  raster_units = NULL
)
}
\arguments{
\item{polygons}{An \code{sf} object containing polygon geometries.}

\item{popup_vars}{Named character vector associating popup labels with
columns in \code{polygons}, such as
\code{c("Municipality" = "municipality", "State" = "state")}.}

\item{popup_title}{Character scalar naming the column used as the popup
title and hover label.}

\item{raster}{Optional single-layer \code{SpatRaster} to display below the
polygons.}

\item{raster_label}{Character scalar describing \code{raster}. Required when
\code{raster} is supplied.}

\item{raster_units}{Character scalar containing the units of \code{raster}.
Required when \code{raster} is supplied.}
}
\value{
A \code{tmap} object in interactive view mode.
}
\description{
Builds an interactive \code{tmap} with polygon boundaries, internal clickable
points and, optionally, a raster layer with a labelled legend.
}
\details{
One point is created inside each polygon with
\code{\link[sf:st_point_on_surface]{sf::st_point_on_surface()}}. Clicking a point displays the attributes
selected by \code{popup_vars}; hovering displays \code{popup_title}.
The function supports \code{tmap} version 4.0 and later. It uses \code{tm_popup()}
when exported by the installed version, otherwise the equivalent
\code{popup.vars} and \code{id} layer arguments.

If \code{raster} has a time coordinate, its first time value is appended to the
map title. The caller remains responsible for selecting the raster layer to
display.
}
\examples{
\dontrun{
municipality_map <- make_interactive_spatial_map(
  polygons = municipalities,
  popup_vars = c(
    "Municipality" = "municipality",
    "State" = "state"
  ),
  popup_title = "municipality"
)

print(municipality_map)
}

}
